#include<iostream>
using namespace std;
int main(){
    int x,y,z;
    cout<<"enter the sides of triangle";
    cin>>x>>y>>z;
    if((x+y>z) && (y+z>x) && (x+z>y)){
        cout<<"they are sides of triangle";
    }
    else
    cout<<"they are sides of triangle";
}